#include "main.h"

/**
 * _abs - function that will compute an absolute value of an integer
 * @m: the int that will be used for function argument
 * Return: 0
 */
int_abs(int m)
{
	if (m > 0 || m == 0)
	{
	return (m);
	}
	else
	return (m * -1);
}
