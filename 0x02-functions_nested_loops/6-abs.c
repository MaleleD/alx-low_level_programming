#include "main.h"

/**
 * _abs - a unction that computes the absolute value of an integer.
 * @m: the int that will be used for the function argument
 * Return: 0
 */
int _abs(int)
{
	if (m > 0 || m == 0)
	{
	 return (m);
	}
	else
	return (m * -1);
}
